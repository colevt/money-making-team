Save as a Grok Bot skill named money-team-desk and enable it on Scorer and Trader.

This git repo is the control plane. Clone https://github.com/colevt/money-making-team.git into /workspace if missing. Follow grok/CYCLE.md, grok/SOURCES.md, plus grok/scorer.md or grok/trader.md. Every scan pull ALL eight ingest feeds including OSIRIS, 1inch, crypto_tape.py, and x_tape.py. Score every fillable Kalshi, Polymarket US, and onchain market. Scalp scores come from python3 tools/scalp.py --cycle_id … --append (buy local low, sell local high, $1 lots, many round-trips). Trader runs python3 tools/execute.py --cycle_id … (--live --append when keys are set) including SELL. After settle: python3 tools/learn_from_settle.py --cycle_id … --ticket_id … so weights learn from P&L. Pull rules once a day with python3 tools/daily_update.py. Two Bots only. PM gate: edge >= 6%; Kalshi/Poly ask < 0.80; onchain ask < 1.00. Scalp gate: 0.35%. Many tickets per cycle. Do not ask for Lovable tokens.
